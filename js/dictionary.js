$('header').load('inc.html > header > .all',change());
$('footer').load('inc.html > footer > .footer');
$('.popup').load('inc.html > .popup > .pop_bg');

function change(text){
    // 제목바꾸는 함수
}

// list 뿌리기
